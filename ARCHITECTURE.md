# System Architecture

## Database Schema

```
┌─────────────────────────────────────────────────────────────────┐
│                     MESSAGING SYSTEM SCHEMA                      │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────┐         ┌──────────────────────────┐
│      users       │         │     conversations        │
├──────────────────┤         ├──────────────────────────┤
│ id               │         │ id                       │
│ name             │         │ type (private/group)     │
│ email            │         │ name (nullable)          │
│ password         │         │ created_by (FK → users)  │
│ ...              │         │ timestamps               │
└──────────────────┘         └──────────────────────────┘
        │                              │
        │                              │
        │    ┌─────────────────────────┴──────────────┐
        │    │                                        │
        │    ▼                                        ▼
        │  ┌──────────────────────────┐    ┌──────────────────┐
        │  │ conversation_participants│    │    messages      │
        │  ├──────────────────────────┤    ├──────────────────┤
        └─►│ id                       │    │ id               │
           │ conversation_id (FK)     │◄───│ conversation_id  │
           │ user_id (FK)             │    │ user_id (FK)     │
           │ last_read_at             │    │ body             │
           │ timestamps               │    │ type             │
           └──────────────────────────┘    │ file_path        │
                                           │ timestamps       │
                                           └──────────────────┘
                                                    │
                                                    │
                                           ┌────────┴────────┐
                                           │     calls       │
                                           ├─────────────────┤
                                           │ id              │
                                           │ conversation_id │
                                           │ initiated_by    │
                                           │ type (audio/vid)│
                                           │ status          │
                                           │ screen_sharing  │
                                           │ started_at      │
                                           │ ended_at        │
                                           │ timestamps      │
                                           └─────────────────┘
```

## Application Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER JOURNEY                              │
└─────────────────────────────────────────────────────────────────┘

1. USER LOGS IN
   │
   ├─► Clicks "Messages" in Navigation
   │
   ▼
2. CONVERSATIONS LIST (/conversations)
   │
   ├─► View existing conversations
   ├─► Click "New Conversation"
   │   │
   │   ├─► Select "Private Message"
   │   │   └─► Choose user → Create 1-on-1 chat
   │   │
   │   └─► Select "Group Chat"
   │       └─► Enter name + Select users → Create group
   │
   ▼
3. CONVERSATION VIEW (/conversations/{id})
   │
   ├─► View message history
   ├─► Send text message
   ├─► Upload file (image/audio/video)
   ├─► Initiate audio call 🎤
   ├─► Initiate video call 📹
   │
   ▼
4. REAL-TIME UPDATES (Optional)
   │
   └─► Messages appear instantly
       └─► Call notifications
```

## API Request Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                     REQUEST → RESPONSE FLOW                      │
└─────────────────────────────────────────────────────────────────┘

CLIENT                    ROUTE                  CONTROLLER
  │                         │                         │
  │  POST /conversations    │                         │
  ├────────────────────────►│                         │
  │                         │  ConversationController │
  │                         ├────────────────────────►│
  │                         │                         │ Create
  │                         │                         │ Conversation
  │                         │                         │
  │                         │      ◄──────────────────┤
  │  Redirect to chat       │                         │
  │◄────────────────────────┤                         │
  │                         │                         │
  │                         │                         │
  │  POST /conversations/   │                         │
  │       {id}/messages     │                         │
  ├────────────────────────►│                         │
  │                         │    MessageController    │
  │                         ├────────────────────────►│
  │                         │                         │ Store
  │                         │                         │ Message
  │                         │                         │
  │                         │                         │ Broadcast
  │                         │                         │ Event
  │                         │      ◄──────────────────┤
  │  Message saved          │                         │
  │◄────────────────────────┤                         │
  │                         │                         │
  │                         │                         │
  │  POST /conversations/   │                         │
  │       {id}/calls        │                         │
  ├────────────────────────►│                         │
  │                         │     CallController      │
  │                         ├────────────────────────►│
  │                         │                         │ Create
  │                         │                         │ Call
  │                         │                         │
  │                         │                         │ Broadcast
  │                         │                         │ Event
  │                         │      ◄──────────────────┤
  │  Call initiated (JSON)  │                         │
  │◄────────────────────────┤                         │
```

## Component Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    LARAVEL APPLICATION                           │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│                         FRONTEND                              │
├──────────────────────────────────────────────────────────────┤
│  Blade Templates                                             │
│  ├─ conversations/index.blade.php (List)                     │
│  ├─ conversations/show.blade.php (Chat)                      │
│  └─ components/layout.blade.php (Navigation)                 │
│                                                              │
│  JavaScript (Optional)                                       │
│  └─ messaging-examples.js (API helpers)                      │
└──────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│                         ROUTES                                │
├──────────────────────────────────────────────────────────────┤
│  web.php                                                     │
│  ├─ GET  /conversations                                      │
│  ├─ POST /conversations                                      │
│  ├─ GET  /conversations/{id}                                 │
│  ├─ POST /conversations/{id}/messages                        │
│  ├─ POST /conversations/{id}/calls                           │
│  └─ ... (11 total routes)                                    │
└──────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│                      MIDDLEWARE                               │
├──────────────────────────────────────────────────────────────┤
│  ├─ Authentication (auth)                                    │
│  ├─ CSRF Protection                                          │
│  └─ Authorization (policies)                                 │
└──────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│                      CONTROLLERS                              │
├──────────────────────────────────────────────────────────────┤
│  ├─ ConversationController (CRUD)                            │
│  ├─ MessageController (Send/Delete)                          │
│  └─ CallController (Initiate/Answer/End)                     │
└──────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│                       POLICIES                                │
├──────────────────────────────────────────────────────────────┤
│  ├─ ConversationPolicy (view, update)                        │
│  └─ MessagePolicy (delete)                                   │
└──────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│                        MODELS                                 │
├──────────────────────────────────────────────────────────────┤
│  ├─ Conversation (relationships, scopes)                     │
│  ├─ Message (relationships)                                  │
│  ├─ Call (relationships)                                     │
│  └─ User (updated with relationships)                        │
└──────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│                       DATABASE                                │
├──────────────────────────────────────────────────────────────┤
│  SQLite (database.sqlite)                                    │
│  ├─ conversations                                            │
│  ├─ messages                                                 │
│  ├─ conversation_participants                                │
│  └─ calls                                                    │
└──────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│                    BROADCASTING (Optional)                    │
├──────────────────────────────────────────────────────────────┤
│  Events                                                      │
│  ├─ MessageSent (broadcast to conversation channel)          │
│  └─ CallInitiated (broadcast to conversation channel)        │
│                                                              │
│  Channels                                                    │
│  └─ conversation.{id} (presence channel)                     │
└──────────────────────────────────────────────────────────────┘
```

## File Structure

```
chipper/
│
├── app/
│   ├── Events/
│   │   ├── CallInitiated.php          ← Broadcast call events
│   │   └── MessageSent.php            ← Broadcast message events
│   │
│   ├── Http/Controllers/
│   │   ├── CallController.php         ← Call management
│   │   ├── ConversationController.php ← Conversation CRUD
│   │   └── MessageController.php      ← Message operations
│   │
│   ├── Models/
│   │   ├── Call.php                   ← Call model
│   │   ├── Conversation.php           ← Conversation model
│   │   ├── Message.php                ← Message model
│   │   └── User.php                   ← Updated with relationships
│   │
│   └── Policies/
│       ├── ConversationPolicy.php     ← Conversation authorization
│       └── MessagePolicy.php          ← Message authorization
│
├── database/
│   └── migrations/
│       ├── 2026_01_15_114050_create_conversations_table.php
│       ├── 2026_01_15_114054_create_messages_table.php
│       ├── 2026_01_15_114058_create_conversation_participants_table.php
│       └── 2026_01_15_114103_create_calls_table.php
│
├── resources/
│   └── views/
│       ├── conversations/
│       │   ├── index.blade.php        ← Conversation list
│       │   └── show.blade.php         ← Chat interface
│       └── components/
│           └── layout.blade.php       ← Updated navigation
│
├── routes/
│   └── web.php                        ← 11 new routes
│
├── public/
│   └── js/
│       └── messaging-examples.js      ← API usage examples
│
└── Documentation/
    ├── QUICK_START.md                 ← User guide
    ├── MESSAGING_FEATURES.md          ← Feature docs
    ├── API_REFERENCE.md               ← API reference
    ├── IMPLEMENTATION_SUMMARY.md      ← Technical overview
    ├── CHECKLIST.md                   ← Implementation checklist
    └── IMPLEMENTATION_COMPLETE.md     ← Final summary
```

## Technology Stack

```
┌─────────────────────────────────────────────────────────────────┐
│                      TECHNOLOGY STACK                            │
└─────────────────────────────────────────────────────────────────┘

Backend:
  ├─ Laravel 11.x (PHP Framework)
  ├─ Eloquent ORM (Database)
  ├─ Laravel Broadcasting (Real-time)
  └─ Laravel Storage (File uploads)

Frontend:
  ├─ Blade Templates (Server-side rendering)
  ├─ Tailwind CSS / DaisyUI (Styling)
  └─ Vanilla JavaScript (Interactions)

Database:
  └─ SQLite (Development)
      └─ Can be changed to MySQL/PostgreSQL

File Storage:
  └─ Local Storage (storage/app/public)
      └─ Can be changed to S3/CloudFront

Optional Integrations:
  ├─ Pusher / Laravel Reverb (Broadcasting)
  ├─ Twilio / Agora / Daily.co (WebRTC)
  └─ Redis (Caching & Queues)
```

## Security Model

```
┌─────────────────────────────────────────────────────────────────┐
│                      SECURITY LAYERS                             │
└─────────────────────────────────────────────────────────────────┘

1. Authentication
   └─ Laravel Sanctum / Session-based auth

2. Authorization
   ├─ ConversationPolicy
   │  ├─ view: User must be participant
   │  └─ update: User must be participant (groups only)
   │
   └─ MessagePolicy
      └─ delete: User must be message author

3. CSRF Protection
   └─ All POST/PUT/DELETE requests require CSRF token

4. File Upload Validation
   ├─ Max size: 10MB
   ├─ Type validation
   └─ Storage in isolated directory

5. SQL Injection Protection
   └─ Eloquent ORM with parameter binding

6. XSS Protection
   └─ Blade template escaping
```
